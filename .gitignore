node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
# Paraglide
src/lib/paraglide
project.inlang/cache/

# Fallow. The JSON files are the quality baselines the pre-push audit compares
# against, and they are tracked — force-added once, because the tool writes a
# `.fallow/.gitignore` of `*` on every run and would otherwise hide them. The
# caches stay out.
.fallow/*.bin
.fallow/cache

# vitest coverage, produced for the fallow audit
coverage/
